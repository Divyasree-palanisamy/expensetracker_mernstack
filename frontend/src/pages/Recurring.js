import React from 'react';

const Recurring = () => (
    <div style={{ background: '#181818', color: '#fff', minHeight: '100vh', padding: '2rem' }}>
        <h1>Recurring Expenses</h1>
        <div style={{ margin: '2rem 0' }}>
            {/* Placeholder for recurring transaction list and insights */}
            <div style={{ background: '#222', borderRadius: '8px', padding: '1rem', marginBottom: '1rem' }}>
                <h2>Recurring Transactions</h2>
                <p>List of recurring transactions will appear here.</p>
            </div>
            <div style={{ background: '#222', borderRadius: '8px', padding: '1rem' }}>
                <h2>Recurring Insights</h2>
                <p>Insights about recurring expenses will appear here.</p>
            </div>
        </div>
    </div>
);

export default Recurring; 